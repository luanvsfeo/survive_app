<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.survivign_in_columbin">

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconteste"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iconteste"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Conquistas"></activity>
        <activity android:name=".Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Conversa" />
        <activity android:name=".Carregar"/>
    </application>

</manifest>